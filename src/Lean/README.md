# DenseCheck in Lean

DenseCheck is a property-based testing library for F#. It allows you to generate test cases from a dense enumeration of possible values, which can be particularly useful for testing complex data structures or algorithms.

This implementation of DenseCheck is in Lean and the point here is to explore 
the ideas behind DenseCheck in a different language that allows us to prove properties about the implementation itself.

